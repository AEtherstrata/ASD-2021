#ifndef _COUNTING_SORT_H
#define _COUNTING_SORT_H

#include "common.h"

void counting_sort(int* inputArray, int* outputArray, int length, int max_val);

void begin_counting_sort(int* inputArray, int* outputArray, int length);

#endif